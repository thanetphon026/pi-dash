# ================================================
# Pi-Dash configuration (edit and save as .env)
# 0 = secure (require API token), 1 = public (no token for trusted LAN)
PI_DASH_PUBLIC=0
# secret token for secure mode
PI_DASH_TOKEN=changeme
# bind host/port
PI_DASH_HOST=0.0.0.0
PI_DASH_PORT=8080
#Adjust the delay time
PI_DASH_INTERVAL=30
# ================================================
